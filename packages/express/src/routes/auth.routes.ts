/**
 * PEZHWAN — auth router facade.
 *
 * Modular mirror of ./routes.ts. Mount at /v1/auth
 * (/register /login /logout /refresh /otp/* /password/* /email/verify).
 */

export { createAuthRouter } from '../routes.ts';
