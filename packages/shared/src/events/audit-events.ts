/**
 * PEZHWAN — audit event catalog.
 */

export { AUDIT_EVENT, type AuditEventType } from '../types.ts';
export {
  AUDIT_EVENT_SEVERITY,
  type AuditEventType as SeverityClassifiedAuditEvent,
} from '../constants/audit-events.ts';
