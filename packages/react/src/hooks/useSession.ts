/**
 * PEZHWAN — session hook.
 *
 * Mirror of useSession() (sessions list + revocation) from the package entry.
 */

export { useSession } from '../index.tsx';
export type { SessionApi } from '../index.tsx';
