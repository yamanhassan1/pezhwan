{
  "name": "@pezhwan/react",
  "version": "0.1.0",
  "description": "Pezhwan React SDK — PezhwanProvider, useAuth(), useSession(), useAuthorization(), ProtectedRoute, RequireRole, RequirePermission.",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "peerDependencies": {
    "react": "^18.0.0 || ^19.0.0",
    "react-dom": "^18.0.0 || ^19.0.0"
  },
  "dependencies": {
    "@pezhwan/shared": "*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^26.4.0",
    "@types/react": "^18.3.0"
  }
}
