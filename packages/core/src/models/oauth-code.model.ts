/**
 * PEZHWAN — OAuth authorization code model.
 *
 * Higher-level mirror of the authorization code persisted store; re-uses the
 * canonical mongoose schema.
 */
export { AuthorizationCodeDoc, AuthorizationCodeModel } from './authorizationCode.model.ts';