/**
 * PEZHWAN — Permission model.
 *
 * A standalone permission resource; mirrors the permission schema surfaced by
 * the role model while keeping RBAC resources explicit.
 */
export { PermissionDoc, PermissionModel } from './role.model.ts';